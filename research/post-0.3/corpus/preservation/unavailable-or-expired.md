# Unavailable or expired targets

**None.** All 19 target artifacts were reported `expired: false` by GitHub and downloaded successfully during the observation window. Each downloaded ZIP passed integrity testing and matched GitHub's reported size and SHA-256.

The remote expiration timestamps are retained in `artifact-ledger.json`. In particular, source-export artifact `9275193303` was scheduled to expire at `2026-08-18T03:17:27Z`; this archive preserves its downloaded bytes independently of later GitHub availability.

This result describes availability at capture time only. It does not claim the corresponding GitHub downloads will remain available.
